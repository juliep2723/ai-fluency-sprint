import type { Metadata } from "next";

export const metadata: Metadata = {
    title: "Cheat Codes For Real Life | Free PDF Download",
    description: "Copy-paste prompts that handle the stuff you're too tired to deal with yourself. Free PDF.",
};

export default function SidekickInsp10g4Layout({
    children,
}: {
    children: React.ReactNode;
}) {
    return <>{children}</>;
}
